# KnowWhy 迭代计划 · 2026-09-12

> 本文档由 2026-09-12 的初版搭建会话生成，记录后续迭代方向与待办事项。
> 使用规范：所有未来迭代计划、待办、功能规划统一放在 `play/` 目录，按日期命名（如 `play/2026-09-20-plan.md`）。

## 一、v0.1 初版状态(本次完成)

- [x] Astro minimal 项目初始化（knowwhy，site: https://knowwhy.zyhorg.cn）
- [x] 依赖安装：@astrojs/mdx、@astrojs/cloudflare、@astrojs/sitemap、@astrojs/react、sharp、reicon-react、@fontsource-variable/geist、typescript、astro-seo
- [x] 内容集合 `src/content.config.ts`（why 集合，含 id/title/category/tags/date/status/sources/related 字段）
- [x] 示例文章 WHY.0001：`src/content/why/tech/why-airplane-windows-round.mdx`
- [x] 路由：`/`、`/[category]/`（6 分类）、`/[category]/[...slug]/`、`/about`、`/submit`
- [x] 全局样式：Geist 可变字体 + 深靛蓝 `#1e293b` / 琥珀黄 `#f59e0b` 品牌配色
- [x] SEO：astro-seo 基础 meta + openGraph + canonical + sitemap；首页 FAQPage 与文章页 Article 的 JSON-LD 注入位置已预留
- [x] 性能：仅图标/交互 React 组件水合（BackToTop 使用 client:load，CategoryIcon 仅 SSR）；og-default.png 社交分享图已生成

## 一·五、v0.2 已交付(2026-09-12 第二轮)

- [x] dev 报错根治：`@astrojs/cloudflare` 的 optimizeDeps 清单缺 `astro/zod` 导致 React 双实例 → 新增 configEnvironment 预热插件；BackToTop 改 `client:only`
- [x] 阅读字号:文章页小/中/大切换器(localStorage 记忆)+ 标题正文整体放大
- [x] 内容线扩展:新增 KNOW 编号线(WHY=为什么、Know=你知道吗);`/why/` 归档、`/know/` 归档与详情页
- [x] 站点新闻:`/news/` 列表与详情;发布方式=复制 `src/content/news/template.mdx`,把 `status` 改为 `published`
- [x] 顶部导航 6 入口(首页/WHY/Know/新闻/关于/提交问题)+ 移动端两行布局
- [x] 页脚重设计(深色四栏:品牌/内容线/分类/关于 + 署名行)

> 内容发布速查:WHY 放 `src/content/why/<分类>/<slug>.mdx`;KNOW 放 `src/content/know/<slug>.mdx`;新闻放 `src/content/news/<slug>.mdx`。均可复制同目录 `template.mdx`,改 `status: published` 即发布。

## 二、待办事项(按优先级)

### P1 · 内容与数据

- [ ] 补充更多「为什么」文章（建议先覆盖 6 个分类各 1 篇，验证分类页与相关阅读）
- [ ] 为文章增加 `related` 交叉引用，完善「相关阅读」链路
- [ ] 内容校验：确认 `date` 字段在 Sätteri YAML 解析下正常（若报错改用 `z.coerce.date()`）

### P2 · Supabase 接入(预留位置见代码内 TODO 注释)

- [ ] 投票功能：文章页投票按钮 + `votes` 表（`src/pages/[category]/[...slug].astro` 有占位）
- [ ] 问题提交：`/submit` 表单写入 `why_submissions` 表（`src/pages/submit.astro` 有占位），配合 RLS + Cloudflare Turnstile 防滥用
- [ ] 本周热门：基于投票/浏览统计数据生成（`src/pages/index.astro` 有占位）

> 搜索 `TODO: Supabase integration` 可定位全部预留位置。

### P3 · 体验增强

- [ ] 站内搜索（可基于 Pagefind 纯静态方案，避免引入后端）
- [ ] RSS / Atom 订阅
- [ ] 文章目录（TOC）与阅读进度条
- [ ] 深色模式（品牌色已有深靛蓝底，扩展成本低）

### P4 · 工程化

- [ ] Cloudflare Pages 部署配置与首次上线
- [ ] 将 Node 升级至 >= 22.19.0（消除 undici EBADENGINE 警告）
- [ ] 补充 lint / format（推荐 Biome，零配置且与 Astro 兼容）

## 三、决策记录

| 日期 | 决策 | 原因 |
| --- | --- | --- |
| 2026-09-12 | 内容集合文件用 `src/content.config.ts` 而非 `src/content/config.ts` | Astro 6+ 强制 Content Layer API，旧路径已废弃 |
| 2026-09-12 | 字体包用 `@fontsource-variable/geist` | `geist-sans` 包名不存在，官方现行包名为 `geist`，字体品牌一致 |
| 2026-09-12 | 保留 @astrojs/cloudflare 依赖但主流程为纯静态构建 | 用户要求安装；静态站部署 Cloudflare Pages 无需 adapter，后续如需 SSR/表单接口再启用 |
| 2026-09-12 | astro.config.mjs 加 Vite 插件把 `astro/zod` 预热进 SSR 预打包 | 适配器 optimizeDeps 清单缺口会引发冷启动 reload,造成 React 双实例与 Invalid hook call;dedupe 无法根治 |
